# Production requirements - extends base requirements.txt
# Only includes packages needed specifically for production deployment
-r requirements.txt
gunicorn==21.2.0  # Additional WSGI server option
# Note: daphne and whitenoise already included in requirements.txt